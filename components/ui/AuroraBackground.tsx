type Props = {
  variant?: 'default' | 'warm';
  className?: string;
};

/* Creative Giants is a flat, shadowless cream system — depth comes from
   whitespace and scale, not glowing blobs. Neutralized to a no-op while
   preserving the call signature used across pages. */
export function AuroraBackground(_props: Props) {
  return null;
}
