/* Disabled in the light editorial system — the warm paper canvas carries the
   texture on its own. Kept as a no-op to avoid touching the layout tree. */
export function NoiseOverlay() {
  return null;
}
